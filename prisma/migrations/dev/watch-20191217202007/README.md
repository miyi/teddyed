# Migration `watch-20191217202007`

This migration has been generated by miyi at 12/17/2019, 8:20:07 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE UNIQUE INDEX "lift"."Instructor.email" ON "Instructor"("email")
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration watch-20191217201956..watch-20191217202007
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url      = "file:dev.db"
 }
 model User {
   id      String    @default(cuid()) @id
@@ -44,9 +44,9 @@
 }
 model Instructor {
   id        String   @default(cuid()) @id
-  email String 
+  email String @unique
   createdAt DateTime @default(now())
   updatedAt DateTime @updatedAt
   firstName String
   lastName  String
```

## Photon Usage

You can use a specific Photon built for this migration (watch-20191217202007)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/watch-20191217202007'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
