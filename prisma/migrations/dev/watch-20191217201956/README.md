# Migration `watch-20191217201956`

This migration has been generated by miyi at 12/17/2019, 8:19:56 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "lift"."new_Instructor" (
  "age" INTEGER NOT NULL DEFAULT 0  ,
  "createdAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  "email" TEXT NOT NULL DEFAULT ''  ,
  "firstName" TEXT NOT NULL DEFAULT ''  ,
  "gender" TEXT NOT NULL DEFAULT 'MALE'  ,
  "id" TEXT NOT NULL   ,
  "lastName" TEXT NOT NULL DEFAULT ''  ,
  "listing" TEXT    REFERENCES "Listing"(id) ON DELETE SET NULL,
  "provider" TEXT NOT NULL   REFERENCES "Provider"(id) ON DELETE RESTRICT,
  "updatedAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  PRIMARY KEY ("id")
);

INSERT INTO "new_Instructor" ("age","createdAt","firstName","gender","id","lastName","listing","provider","updatedAt") SELECT "age","createdAt","firstName","gender","id","lastName","listing","provider","updatedAt" from "Instructor"

DROP TABLE "lift"."Instructor";

ALTER TABLE "lift"."new_Instructor" RENAME TO "Instructor";

PRAGMA "lift".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration watch-20191217191523..watch-20191217201956
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url      = "file:dev.db"
 }
 model User {
   id      String    @default(cuid()) @id
@@ -44,8 +44,9 @@
 }
 model Instructor {
   id        String   @default(cuid()) @id
+  email String 
   createdAt DateTime @default(now())
   updatedAt DateTime @updatedAt
   firstName String
   lastName  String
```

## Photon Usage

You can use a specific Photon built for this migration (watch-20191217201956)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/watch-20191217201956'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
