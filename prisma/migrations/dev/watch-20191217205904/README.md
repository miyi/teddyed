# Migration `watch-20191217205904`

This migration has been generated by miyi at 12/17/2019, 8:59:05 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "lift"."User" (
  "email" TEXT NOT NULL DEFAULT ''  ,
  "id" TEXT NOT NULL   ,
  "name" TEXT    ,
  PRIMARY KEY ("id")
);

CREATE TABLE "lift"."Student" (
  "LastName" TEXT    ,
  "age" INTEGER    ,
  "event" TEXT    REFERENCES "Event"(id) ON DELETE SET NULL,
  "firstName" TEXT    ,
  "gender" TEXT NOT NULL DEFAULT 'MALE'  ,
  "id" TEXT NOT NULL   ,
  "user" TEXT NOT NULL   REFERENCES "User"(id) ON DELETE RESTRICT,
  PRIMARY KEY ("id")
);

CREATE TABLE "lift"."Post" (
  "author" TEXT NOT NULL   REFERENCES "User"(id) ON DELETE RESTRICT,
  "content" TEXT    ,
  "createdAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  "id" TEXT NOT NULL   ,
  "published" BOOLEAN NOT NULL DEFAULT true  ,
  "title" TEXT NOT NULL DEFAULT ''  ,
  "updatedAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  PRIMARY KEY ("id")
);

CREATE TABLE "lift"."Provider" (
  "email" TEXT NOT NULL DEFAULT ''  ,
  "id" TEXT NOT NULL   ,
  "name" TEXT    ,
  PRIMARY KEY ("id")
);

CREATE TABLE "lift"."Instructor" (
  "age" INTEGER NOT NULL DEFAULT 0  ,
  "createdAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  "email" TEXT    ,
  "firstName" TEXT NOT NULL DEFAULT ''  ,
  "gender" TEXT NOT NULL DEFAULT 'MALE'  ,
  "id" TEXT NOT NULL   ,
  "lastName" TEXT NOT NULL DEFAULT ''  ,
  "listing" TEXT    REFERENCES "Listing"(id) ON DELETE SET NULL,
  "provider" TEXT NOT NULL   REFERENCES "Provider"(id) ON DELETE RESTRICT,
  "updatedAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  PRIMARY KEY ("id")
);

CREATE TABLE "lift"."Event" (
  "createdAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  "from" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  "id" TEXT NOT NULL   ,
  "listing" TEXT NOT NULL   REFERENCES "Listing"(id) ON DELETE RESTRICT,
  "provider" TEXT NOT NULL   REFERENCES "Provider"(id) ON DELETE RESTRICT,
  "to" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  "updatedAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  PRIMARY KEY ("id")
);

CREATE TABLE "lift"."Listing" (
  "content" TEXT    ,
  "createdAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  "id" TEXT NOT NULL   ,
  "owner" TEXT NOT NULL   REFERENCES "Provider"(id) ON DELETE RESTRICT,
  "title" TEXT NOT NULL DEFAULT ''  ,
  "updatedAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  PRIMARY KEY ("id")
);

CREATE TABLE "lift"."_EventsInstructors" (
  "A" TEXT NOT NULL   REFERENCES "Event"(id) ON DELETE CASCADE,
  "B" TEXT NOT NULL   REFERENCES "Instructor"(id) ON DELETE CASCADE
);

CREATE UNIQUE INDEX "lift"."User.email" ON "User"("email")

CREATE UNIQUE INDEX "lift"."Provider.email" ON "Provider"("email")

CREATE UNIQUE INDEX "lift"."Instructor.email" ON "Instructor"("email")

CREATE UNIQUE INDEX "lift"."_EventsInstructors_AB_unique" ON "_EventsInstructors"("A","B")
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration watch-20191217202802..watch-20191217205904
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url      = "file:dev.db"
 }
 model User {
   id      String    @default(cuid()) @id
```

## Photon Usage

You can use a specific Photon built for this migration (watch-20191217205904)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/watch-20191217205904'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
